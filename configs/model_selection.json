{
  "experiment_name": "grouped_model_selection_v1",
  "evaluation_split": "validation",
  "feature_set": "engineered_design_oracle_v1",
  "random_seed": 20260827,
  "cross_validation": {
    "folds": 5,
    "shuffle_groups": true,
    "primary_metric": "mae"
  },
  "selection": {
    "validation_mae_tolerance": 0.005,
    "rule": "lowest_complexity_within_validation_mae_tolerance_then_cv_mae_then_cv_std"
  },
  "random_forest": {
    "fixed": {
      "criterion": "squared_error",
      "n_estimators": 200,
      "n_jobs": -1
    },
    "grid": {
      "max_depth": [8, null],
      "min_samples_leaf": [5, 20],
      "max_features": ["sqrt", 0.75]
    },
    "complexity_rank": 2,
    "interpretability": "moderate: impurity importances and partial dependence are available"
  },
  "gradient_boosting": {
    "fixed": {
      "loss": "squared_error",
      "min_samples_leaf": 20
    },
    "grid": {
      "n_estimators": [100, 200],
      "learning_rate": [0.03, 0.07],
      "max_depth": [2, 3]
    },
    "complexity_rank": 3,
    "interpretability": "moderate-low: sequential trees support global importance but interact nonlinearly"
  },
  "xgboost": {
    "enabled": false,
    "reason": "Excluded from this milestone because it adds a large optional dependency and overlaps the tested gradient-boosting role; revisit only if sklearn ensembles leave a material performance gap."
  }
}
